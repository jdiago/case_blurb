<%= form_for([@case, @case_doc]) do |f| %>
  <% if @case_doc.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@case_doc.errors.count, "error") %> prohibited this doc from being saved:</h2>

      <ul>
      <% @case_doc.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= f.text_field :name %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
